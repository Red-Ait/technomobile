<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.isima.technomobile">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TechnoMobile">
        <activity
            android:name=".activities.DépensesActivity"
            android:label="@string/title_activity_depenses"
            android:theme="@style/Theme.TechnoMobile.NoActionBar" />
        <activity
            android:name=".activities.MembersActivity"
            android:label="@string/title_activity_member"
            android:theme="@style/Theme.TechnoMobile.NoActionBar" />
        <activity
            android:name=".activities.GroupActivity"
            android:label="@string/title_activity_group_"
            android:theme="@style/Theme.TechnoMobile.NoActionBar" />
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.TechnoMobile.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>